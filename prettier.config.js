
module.exports = {
  semi: true, // Usa punto y coma al final de cada línea
  singleQuote: true, // Usa comillas simples en vez de dobles
  tabWidth: 2, // Usa 2 espacios por tabulación
  trailingComma: 'es5', // Agrega coma al final en objetos/arrays ES5
  printWidth: 100 // Máximo de 100 caracteres por línea antes de hacer salto
};
